Anki preserves this directory when the add-on is upgraded.

AI Workspace stores profiles.json and deck_assignments.json here at runtime.
Do not remove this directory from release archives, even when it contains no
profile data yet.
